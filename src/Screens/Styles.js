import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    homeSearchBar: {
        marginTop: 60
    },
    userIcon: {
        position: 'absolute',
        left: 20,
        top: 15
    },
});

export default styles;